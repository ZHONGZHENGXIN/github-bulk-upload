{
  "name": "investment-workflow-backend",
  "build": {
    "commands": [
      "npm ci --include=dev",
      "npm run build"
    ]
  },
  "start": "npm start",
  "env": {
    "NODE_ENV": "production"
  }
}
